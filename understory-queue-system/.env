# Kørsel
NODE_ENV=production

# Redis
REDIS_URL=redis://127.0.0.1:6379

# Hvor brugeren ender når de er "ready" (fallback når token ikke bruges)
QUEUE_REDIRECT_URL=https://lamineyamalerenwanker.app/done

# (Hvis du bruger vores tidligere port-offset-løsning, kan du ignorere nedenstående.
# I jeres nuværende opsætning lytter én proces bare på 3000..3005.)
# BASE_PORT=3000
# PORT_OFFSET=0
# FIXED_PORT=
